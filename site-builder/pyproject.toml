[project]
name = "site-builder"
version = "0.1.0"
description = "Static site generator for insights subprojects"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
